# LiveKit Ingress 原生模式配置 (Windows 研发)
# 用于: 原生 livekit-server + 原生 ingress (容器↔宿主 UDP 受限的替代方案)
api_key: devkey
api_secret: secret
ws_url: ws://localhost:7880

redis:
  address: localhost:6379

rtmp_port: 1935
whip_port: 8080

logging:
  level: info
