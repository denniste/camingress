# LiveKit Ingress 服务配置
api_key: devkey
api_secret: secret
# 原生 livekit-server (Windows 宿主) 时用 host.docker.internal; 全容器部署时用 ws://livekit:7880
ws_url: ws://host.docker.internal:7880

redis:
  address: redis:6379

rtmp_port: 1935
whip_port: 8080

logging:
  level: info
